<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ShowInstaller2__c</fullName>
    <formula>OR(
    NOT(ISBLANK(TEXT(Installer__c))),       
    NOT(ISBLANK(Install_Start_Date__c)),    
    NOT(ISBLANK(Install_Completed_Date__c))
   )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>ShowInstaller2</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
